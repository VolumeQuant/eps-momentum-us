name: Test Private Bot Only

on:
  workflow_dispatch:
    inputs:
      market_date:
        description: 'Market date (YYYY-MM-DD). Leave empty for auto-detect.'
        required: false
        default: ''
      message_version:
        description: 'Message format: v2 or v3 (current)'
        required: false
        default: 'v3'

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure git
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run full flow (private bot only)
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_PRIVATE_ID: ${{ secrets.TELEGRAM_PRIVATE_ID }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          TZ: 'America/New_York'
          MARKET_DATE: ${{ github.event.inputs.market_date }}
          MESSAGE_VERSION: ${{ github.event.inputs.message_version }}
          # TELEGRAM_CHAT_ID 미설정 → 채널 전송 안함, 개인봇만
        run: python daily_runner.py

      # 테스트 워크플로우는 state/DB를 커밋하지 않음 (프로덕션 데이터 오염 방지)
