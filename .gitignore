# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
.eggs/
*.egg-info/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
nul

# Config (contains secrets)
config.json

# Claude Code (contains credentials in command history)
.claude/

# Logs (일일 로그는 Git에 올림)
# logs/  <- 주석처리: 로그도 올림

# 테스트 캐시
*.pkl

# 모든 데이터 파일 Git에 포함 (여러 PC 동기화용)
# eps_data/  <- 주석처리: CSV 올림
# *.db       <- 주석처리: DB 올림
# reports/   <- 주석처리: 리포트 올림

# 임시 백테스트 DB (DB 복사본)
eps_test*.db

# DB 백업 (마이그레이션 전 스냅샷)
eps_momentum_data.db.*backup*
eps_momentum_data.db.before_*
eps_momentum_data.bak_*

# Research 임시 BT DB 폴더 (BT 결과 캐시 — git에 올리지 않음)
research/*_dbs/
research/*_dbs_*/
research/pe_*_dbs/
research/*.db
research/*.bak.db

# 임시 로그
wf_log.txt
